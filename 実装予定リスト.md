# 実装予定リスト（→ 機能一覧.md に集約しました）

**このファイルの内容は [機能一覧.md](機能一覧.md) に移しました。**
実装状況の確認は、そちらをご覧ください。二重管理を避けるため、今後の更新も機能一覧.md で行います。

機能一覧.md には次が入っています。

- ご依頼当初の12項目への対応状況
- 打ち合わせメモ10項目への対応状況
- ご要望メール（1-①〜4-4）への対応状況
- 実装済み機能の一覧（学習／記憶／継続／問題管理／先生・保護者向け／利用環境）
- 未実装（サーバーが必要・ご相談後に着手）
- 未実装（サーバー不要・ご希望があれば追加）
- 実機での確認をお願いしたい項目
- 参考の数値と、修正した不具合の記録

---

## 保守上の注意（開発者向けのメモ）

- **CSSやJavaScriptを更新したら、`sw.js` の `CACHE` の版番号を必ず上げる。** 上げないと利用者の端末で古い画面が表示され続けます。
- サービスワーカーは保存時に必ずサーバーから取り直します（`cache: 'reload'`）。以前は、配信側に残っていた古いファイルが新しい版として保存され、番号を上げても画面が変わらない不具合がありました。
- `questions/` の中の問題ファイルは、キャッシュ優先にすると差し替えが反映されないため、**ネットワーク優先**（通信できないときだけキャッシュ）にしています。
- 記録の保存形式を変えるときは `STORAGE_KEY` も変える。ただし変えると既存の記録が読めなくなるため、移行処理の要否を検討すること。
- 高さを指定する要素は、`span` のままだと効きません（`display: block` が必要）。値の検証だけでは気づけないため、見た目の確認も行うこと。
