<?php
/* ============================================================
   接続の設定（見本）
   ------------------------------------------------------------
   このファイルを config.php という名前でコピーして、
   Xサーバーの管理画面で作った値に書きかえてください。

   ★ config.php は公開リポジトリに置かないでください。
     .gitignore に入れてあります。
   ============================================================ */

return [
    // 手元（XAMPP）で試すときの値です
    'db_host' => '127.0.0.1',
    'db_name' => 'sjshiki',
    'db_user' => 'root',
    'db_pass' => '',
    'db_port' => 3306,

    /*
     * アプリを置く場所（https://example.com/app/ など）。
     * ここに書いたところからの呼び出しだけを受けつけます。
     * 空にすると同じサーバーからの呼び出しだけになります。
     */
    'allow_origin' => '',

    /*
     * アプリを開くときのアドレス。
     * パスワード再設定のリンクを組み立てるのに使います。
     * 例）'https://satojun.org/sj'
     * 空のままだと、メールのリンクが相対のままになり開けません。
     */
    'app_url' => 'https://example.com/sj',

    /*
     * 最初の用意（server/setup.php）を、ブラウザから実行するための合言葉。
     * SSHが使えないサーバー向けの入口です。
     *
     *   1. ここに長い文字列を書く（20文字以上。人が推測できないもの）
     *   2. ブラウザで  .../server/setup.php?token=（その文字列）  を開く
     *   3. 終わったら、必ず '' に戻す
     *
     * ★ 空のままなら、ブラウザからは実行できません。
     *   ふだんは空にしておいてください。
     */
    'setup_token' => '',

    /*
     * 保守用アカウントのログインID。
     * ここに書いたIDは、教室側の名簿とアカウント一覧に出なくなります
     * （そのID自身でログインしているときは、除外せず全部出ます）。
     *
     *   'dev_login_ids' => ['xxxx', 'yyyy'],
     *
     * ★ 実際のIDは、この見本ではなく config.php にだけ書いてください。
     *   この見本は公開リポジトリに入るため、書くとIDが知られてしまいます。
     *
     * 空のまま、または書かなければ、誰も隠しません。
     */
    'dev_login_ids' => [],

];
