/* ============================================================
   つなぎ先の設定
   ------------------------------------------------------------
   サーバーを使うときだけ、ここを書きかえます。

     空のまま     … 端末の中だけで動きます（第1段階と同じ）
     場所を書く   … サーバーにつなぎます（第2段階）

   例）アプリと同じ場所にサーバーを置いた場合
        const SERVER_BASE = 'server/api/';

   ※ 書きかえたら sw.js の CACHE の版番号も上げてください。
     上げないと、すでに開いたことのある端末では古い設定のままになります。
   ============================================================ */

'use strict';

const SERVER_BASE = '';
